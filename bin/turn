#!/usr/bin/env ruby
require_relative '../lib/turn'
board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

puts "Welcome to Tic Tac Toe!"
display_board(board)
turn(board)


  puts "Please enter 1-9:"
  #gets the user input
  input_user = gets.strips
  #input_to_index
  index = input_to_index(input_user)
     valid_move?(board, index)
    move(board, index, player_1(board))
    char = player_1(board)
    else
  turn(board)
  end
end
